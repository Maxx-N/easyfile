<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/documents"
    ><%= isAdmin ? 'Admin' : 'EasyFile' %></a
  >
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarColor02"
    aria-controls="navbarColor02"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor02">
    <% if(isAuthenticated) { %>
    <ul class="navbar-nav mr-auto">
      <% if(isAdmin) { %>
      <li class="nav-item">
        <a
          class="nav-link <%= path === '/admin/doctypes' ? 'active' : '' %>"
          href="/admin/doctypes"
          >DocTypes</a
        >
      </li>
      <% } %>

      <li class="nav-item">
        <a
          class="nav-link <%= path === '/documents' ? 'active' : '' %>"
          href="/documents"
          >Mes documents</a
        >
      </li>
    </ul>
    <% } %> 
    
    <% if (isProAuthenticated) { %>
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a
                  class="nav-link <%= path === '/loan-files' ? 'active' : '' %>"
                  href="/loan-files"
                  >Prêts
                </a
              >
            </li>
        </ul>
    <% } %>

    <ul class="navbar-nav ml-auto">
      <% if (!isAuthenticated && !isProAuthenticated) { %>
      <li
        class="nav-item dropdown <%= path === '/login' || path === '/signup' ? 'active' : '' %>"
      >
        <a
          class="nav-link dropdown-toggle"
          data-toggle="dropdown"
          href="#"
          role="button"
          aria-haspopup="true"
          aria-expanded="false"
          >Authentification</a
        >
        <div class="dropdown-menu">
          <a
            class="dropdown-item <%= path === '/login' ? 'active' : '' %>"
            href="/login"
            >Connexion</a
          >
          <div class="dropdown-divider"></div>
          <a
            class="dropdown-item <%= path === '/signup' ? 'active' : '' %>"
            href="/signup"
            >Inscription</a
          >
        </div>
      </li>
      <% } else { %>
      <li class="nav-item dropdown">
        <a
          class="nav-link dropdown-toggle"
          data-toggle="dropdown"
          href="#"
          role="button"
          aria-haspopup="true"
          aria-expanded="false"
          ><%= isAuthenticated ? user.email : pro.email %></a
        >
        <div class="dropdown-menu">
          <a class="dropdown-item" href="#">Profil</a>
          <div class="dropdown-divider"></div>
          <form method="POST" action="/logout">
            <button class="dropdown-item">Déconnexion</button>
          </form>
        </div>
      </li>
      <% } %>
    </ul>
  </div>
</nav>
